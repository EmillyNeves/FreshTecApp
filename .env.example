# FreshTec Mobile - Configurações de Ambiente

# Ambiente de execução (development, production)
NODE_ENV=development

# Porta do servidor (padrão: 5000)
PORT=5000

# Database (PostgreSQL para produção)
# Exemplo: postgresql://username:password@localhost:5432/database_name
DATABASE_URL=postgresql://user:password@localhost:5432/freshtec

# Configurações de desenvolvimento (opcional)
# Use 'memory' para armazenamento em memória durante desenvolvimento
STORAGE_TYPE=memory

# Configurações de sessão (opcional)
SESSION_SECRET=your_session_secret_here

# Configurações de logging (opcional)
LOG_LEVEL=info

# CORS Origins (opcional, para produção)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# API Keys (se necessário para integração)
# EXTERNAL_API_KEY=your_api_key_here